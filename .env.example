# Supabase 配置（复制此文件为 .env 并填入真实值）
# 从 Supabase 控制台 Settings → API 获取。
# 注意：anon key 是设计上可公开的（前端专用），真正的安全靠数据库 RLS 策略。
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
